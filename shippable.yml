language: c

comoiler:
    - gcc
      
branches:
  only:
    - master

build:
  ci:
      #- apt-get update
      #- apt-get install cmake make g++ libboost-all-dev libgstreamer-plugins-base1.0-dev libgstreamer-plugins-bad1.0-dev
      #- cmake .
      #- cmake --build 
      #- make test


integrations:
  notifications:
    - integrationName: email
      type: email
      on_success: change
      on_failure: always
      on_pull_request: never
